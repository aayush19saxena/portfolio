<h1 id="page-title">Stuff I've Built</h1>

<div id="post-wrapper" class="skinny-wrapper">
	<% @projects.each do |project| %>
		<div class="post">
			<p class="date"><%= project.created_at.strftime('%A, %b %d') %></p>
			<h2><%= link_to project.title, project %></h2>
			<hr>
		</div>
	<% end -%>
</div>